{{- if .Values.worker.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: smart-redact-worker
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "smart-redact.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.worker.port }}
      targetPort: {{ .Values.worker.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "smart-redact.selectorLabels" (dict "component" "worker" "Release" .Release) | nindent 4 }}
{{- end }}
