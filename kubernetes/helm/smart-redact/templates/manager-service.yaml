{{- if .Values.manager.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: smart-redact-manager
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "smart-redact.labels" . | nindent 4 }}
    app.kubernetes.io/component: manager
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.manager.port }}
      targetPort: {{ .Values.manager.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "smart-redact.selectorLabels" (dict "component" "manager" "Release" .Release) | nindent 4 }}
{{- end }}
